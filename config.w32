// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("svn", "for svn support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("svn", "enable svn support", "no");

if (PHP_SVN != "no") {
	EXTENSION("svn", "svn.c");
}

